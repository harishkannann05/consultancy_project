import './Dashboard.css'

function Dashboard() {
  return (
    <div className="dashboard-page">
      <h1>Dashboard</h1>
      <p>Dashboard (built by team). Teammates can replace this component or route with the full dashboard.</p>
    </div>
  )
}

export default Dashboard
